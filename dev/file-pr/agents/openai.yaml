interface:
  display_name: "File Pr"
  short_description: "Run File Pr with clear steps"
  default_prompt: "Use $file-pr to complete the requested workflow."
policy:
  allow_implicit_invocation: true
