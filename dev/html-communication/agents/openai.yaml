interface:
  display_name: "HTML communication"
  short_description: "Publish reports, visual explainers, and UI mock variants"
  default_prompt: "Use $html-communication to present this as an HTML document and publish it to Postplan."
policy:
  allow_implicit_invocation: true
