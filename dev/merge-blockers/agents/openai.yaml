interface:
  display_name: "Merge Blockers"
  short_description: "Triage review findings against the merge-blocker bar"
  default_prompt: "Use $merge-blockers to triage the review findings on this change and report which ones block merge."
policy:
  allow_implicit_invocation: false
