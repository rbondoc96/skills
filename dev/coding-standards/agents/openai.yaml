interface:
  display_name: "Coding Standards"
  short_description: "Standards by language, domain, framework, and tool"
  default_prompt: "Use $coding-standards to select and apply the standards for the languages, responsibilities, frameworks, and tools touched by this task."
policy:
  allow_implicit_invocation: true
